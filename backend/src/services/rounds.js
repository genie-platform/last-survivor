const mongoose = require('mongoose')
const Round = mongoose.model('Round') 
const UserState = mongoose.model('UserState')

// export const determineWinner = (round) => {
//   const users = UserState,find({ })

// }